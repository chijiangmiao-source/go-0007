# 空间碎片观测编目与轨道解算闭环服务

## 项目目标

建设一个可编译、可独立运行的 Go HTTP 后端，接收近地轨道空间碎片的多测站异步观测弧段，并依次完成校验、幂等去重、目标关联、可取消轨道解算、残差质检、人工复核以及发布版本冻结。服务采用事务型持久化、单调事件序号和确定性计算适配器，使乱序、重复、跨午夜、并发上报及进程重启不会改变最终结果。项目只设置六个指定核心组件；HTTP、存储和计算引擎均作为这些组件的接口适配层。生产代码目标约 2500 行，控制在 2000 至 3000 行且不少于 20 个生产 Go 文件。

## 端到端业务流程

1. 观测接入流程：地面站提交测站标识、弧段标识、CCSDS 时间戳、角度采样和置信度；接入 API 完成结构与范围校验，计算规范载荷哈希，并在同一事务中写入观测及接收事件后返回确认结果。
2. 去重与关联流程：关联器按持久化事件序号消费已接收弧段，先识别完全重复、冲突重复和不可用测站数据，再依据时间邻近、角度残差、测站几何及置信度为弧段选择既有目标或建立待确认目标。
3. 轨道解算流程：满足配置化观测条件的目标生成输入快照和解算任务；调度器串行化同一目标的运行任务，调用本地确定性轨道计算替身，记录迭代、超时、取消、不收敛及成功结果。
4. 残差质检流程：成功解算产生逐观测残差与汇总指标；状态机根据两级固定阈值自动通过、转入异常复核或直接拒绝，中间区间由复核决定批准或驳回，任何非法状态迁移均被拒绝并留痕。
5. 冻结与恢复流程：批准的解算可在目标级互斥事务中冻结为不可变发布版本，查询服务读取当前或历史版本；进程重启后从检查点按全局事件序号重放未完成工作，并校验输入快照及结果哈希一致性。

## 核心组件与职责

1. 观测接入 API：解析 HTTP 请求、验证 CCSDS 时间和角度弧段、执行幂等写入，并提供观测接收结果查询。
2. 事件去重与目标关联器：规范化观测、识别重复或冲突载荷，以确定性评分和稳定决胜规则维护弧段与目标的关联。
3. 轨道解算调度器：创建目标输入快照、限制同目标并行任务、传播取消与超时，并封装确定性轨道计算接口。
4. 残差质检状态机：保存逐点和汇总残差，执行阈值分支、人工复核决定及严格的合法状态迁移。
5. 版本冻结与查询服务：以目标级互斥和期望修订号冻结批准解算，保证发布版本不可变并提供当前及历史查询。
6. 持久化及重启恢复模块：事务保存领域记录、事件日志、检查点和哈希，负责崩溃后的有序重放与未完成任务恢复。

## 领域规则与不变量

1. 每个观测弧段必须引用已登记测站，包含唯一弧段标识、至少三个按观测时刻可排序的角度采样以及零到一之间的置信度。
2. CCSDS 时间值必须带明确 UTC 标识并可无歧义解析；内部统一为 UTC 纳秒时间，跨午夜按绝对时刻排序，同时保留原始文本供审计。
3. 方位角取值为大于等于零且小于 360 度，仰角取值为负 90 至正 90 度；非有限数、反向弧段边界或越界采样必须拒绝。
4. 幂等身份由测站标识和弧段标识组成；相同身份且规范载荷哈希相同视为成功重复，不得产生第二条观测、关联或解算事件。
5. 相同幂等身份对应不同载荷哈希属于冲突重复，返回冲突错误并保留原记录，禁止静默覆盖或合并两个版本。
6. 关联评分只使用已冻结的配置、规范输入和确定性数学；分数相同依次按较小残差、较早目标创建序号和目标标识决胜。
7. 超过允许时钟漂移阈值的测站弧段可持久化但进入隔离状态，在漂移解除或获得复核结论前不得参与目标关联和轨道解算。
8. 同一目标同一时刻只能有一个运行中的解算任务；任务输入必须固定关联修订、弧段集合、阈值配置及计算器版本。
9. 解算状态仅允许排队转运行或取消，运行转成功、失败、超时或取消；取消及其他终态均不可重新进入运行状态。
10. 残差低于发布阈值自动批准，处于发布与拒绝阈值之间进入待复核，高于拒绝阈值直接驳回；仅待复核结果接受人工决定。
11. 只有批准且尚未冻结的解算可以生成发布版本；同一目标的版本号严格递增，并通过期望当前版本实现并发冻结互斥。
12. 冻结版本的轨道参数、输入引用、残差、配置和结果哈希永久不可修改；任何修正必须重新解算并冻结为后续版本。

## 数据模型与持久化

1. Station：测站标识、名称、位置坐标、启用状态、允许时钟漂移、当前漂移估计和配置修订号。
2. ObservationArc：测站与弧段标识、原始及规范时间、角度采样、置信度、载荷哈希、接收序号和隔离状态。
3. CatalogTarget：内部目标标识、创建事件序号、当前关联修订、生命周期状态以及当前冻结版本号。
4. AssociationDecision：弧段、候选目标、评分明细、决胜依据、最终目标、配置修订和决定事件序号。
5. SolveJob：任务标识、目标与关联修订、输入快照哈希、计算器版本、状态、尝试次数、期限和取消信息。
6. OrbitSolution：解算任务、轨道历元、确定性轨道参数、迭代摘要、收敛结果、输出哈希和生成时间。
7. ResidualReview：解算标识、逐观测残差、汇总指标、阈值快照、质检状态、复核决定及理由。
8. FrozenVersion：目标、递增版本号、解算引用、冻结事件序号、完整内容哈希和不可变发布时间。
9. EventRecordAndCheckpoint：全局单调序号、事件类型、聚合标识、规范载荷、因果标识、校验和及恢复检查点。

## 公开接口

1. POST /v1/observation-arcs：提交弧段；成功新建返回 202，完全重复返回原确认，冲突重复返回 409，校验失败返回 422。
2. GET /v1/observation-arcs/{arc_id}：返回弧段校验、隔离、去重和目标关联状态，不暴露可变内部对象。
3. GET /v1/targets/{target_id}：查询目标、关联修订、解算摘要、质检状态和当前冻结版本引用。
4. POST /v1/targets/{target_id}/solve-jobs：基于期望关联修订创建幂等解算任务，修订过期时返回冲突。
5. GET /v1/solve-jobs/{job_id}：查询任务输入摘要、状态、尝试次数、期限、失败分类及结果引用。
6. POST /v1/solve-jobs/{job_id}/cancel：幂等请求取消排队或运行任务，并返回取消是否先于终态生效。
7. GET /v1/solutions/{solution_id}/residuals：返回逐点残差、汇总指标、阈值快照和当前质检状态。
8. POST /v1/solutions/{solution_id}/review-decisions：仅为待复核解算提交批准或驳回决定及审计理由。
9. POST /v1/targets/{target_id}/versions/freeze：携带解算标识和期望当前版本，在互斥事务中冻结新版本。
10. GET /v1/targets/{target_id}/versions：按版本号返回不可变历史摘要，并明确标识当前发布版本。
11. GET /v1/targets/{target_id}/versions/{version}：返回指定冻结版本、来源观测引用、残差摘要和内容哈希。
12. OrbitEngine 内部接口：接收不可变输入快照、截止时间和取消信号，返回确定性解算结果或分类错误。

## 失败边界

1. 格式、时间或数值校验失败终止于接入边界，只记录拒绝指标，不创建可参与后续流程的观测事件。
2. 重复上报由唯一约束和事务共同收敛；并发请求不得绕过去重，冲突载荷也不得污染已确认观测。
3. 某一目标的关联或解算故障只能阻塞该目标，其他测站和目标的接收、关联及查询必须继续工作。
4. 测站时钟漂移只隔离受影响测站的新增弧段，已冻结版本保持可查询且不得因后续校时被重写。
5. 计算超时和可重试执行错误采用有上限的退避重试；数值不收敛属于明确终态，不进行无限重试。
6. 取消与解算完成竞争时，以首个成功提交的终态事务为准；迟到结果不得覆盖已生效的取消状态。
7. 残差异常、非法复核或非法状态迁移不得进入冻结服务，错误必须带当前状态和可追踪事件标识。
8. 并发冻结通过目标修订条件更新解决，只有一个请求成功；失败请求重新读取，不得复用已分配版本号。
9. 持久化中断时回滚整个领域事务；重启按事件序号重放，遇到校验和不符立即停止恢复并报告损坏边界。

## 验收标准

1. 项目可通过 go build ./... 与 go test ./...，可由单一配置启动 HTTP 服务；默认使用本地事务型存储和确定性轨道计算替身，不依赖外部网络即可运行验收。
2. 非测试、非生成的生产 Go 代码不少于 2000 行且控制在 2000 至 3000 行附近，目标约 2500 行；至少包含 20 个职责清晰的生产 Go 文件，并且核心组件严格限定为计划中的六个。
3. 有效弧段、缺字段、越界角度、非法置信度、CCSDS 时间和跨午夜样例均得到明确结果；同一弧段的串行及并发重复提交只形成一条有效处理链。
4. 相同输入、配置和初始存储快照重复运行至少 100 次，关联目标、决胜理由、事件顺序和输入快照哈希完全一致。
5. 解算的成功、不收敛、超时、重试和取消均遵循状态图；残差三分支及人工复核只能产生允许的后继状态。
6. 并发冻结同一目标时恰有一个请求创建下一版本；冻结后所有写入尝试均失败，历史和当前查询返回的内容哈希保持不变。
7. 在接入、关联、运行解算、质检和冻结事务边界模拟崩溃，重启后不得丢失或重复业务效果，最终事件序列和结果哈希与无故障基线一致。
8. 验收夹具包含公开空间目标观测样例、CCSDS 时间格式样例和本地确定性轨道计算替身；至少十个测试场景拥有互不共享故障开关的独立缺陷验证空间。

## 确定性测试场景

1. DV-01 使用公开空间目标多测站样例提交合法弧段，验证规范化、持久化、关联和查询形成完整闭环。
2. DV-02 分别注入缺字段、NaN、越界角度、置信度越界和弧段边界反转，验证全部在接入边界拒绝。
3. DV-03 对同一合法弧段发起高并发重复提交，验证响应可重放且观测、事件和后续任务各只有一份。
4. DV-04 使用相同测站和弧段标识提交不同载荷，验证返回冲突且原始载荷哈希及关联保持不变。
5. DV-05 乱序提交跨午夜 CCSDS 时间样例，验证按绝对观测时刻和稳定事件序号处理，不按到达顺序误排。
6. DV-06 构造两个关联评分相同的候选目标，反复扰动并发和容器遍历顺序，验证稳定决胜结果一致。
7. DV-07 并发上报多个测站数据并使一个测站超过时钟漂移阈值，验证仅其弧段隔离且其他目标继续解算。
8. DV-08 在排队、刚开始运行和即将完成三个时点取消任务，验证终态竞争规则且无迟到结果覆盖。
9. DV-09 令确定性计算替身返回数值不收敛，验证任务进入失败终态、不无限重试且不会产生可冻结解算。
10. DV-10 注入可重试错误与执行超时，验证退避次数上限、截止时间传播、最终错误分类和任务状态合法。
11. DV-11 生成低于发布阈值的残差，验证自动批准并保留逐点残差、阈值快照和确定性结果哈希。
12. DV-12 分别生成中间区间和高于拒绝阈值的残差，验证人工复核分支、直接驳回及非法决定被拒绝。
13. DV-13 并发冻结同一批准解算并尝试修改已冻结内容，验证单一新版本、版本号连续和不可变约束。
14. DV-14 在各事务边界截断进程后重启重放，验证事件顺序、关联、任务终态、冻结版本和哈希等同基线。

## 组件追踪关系

1. 观测接入 API 对应接入流程，读写 Station 与 ObservationArc，承载观测 POST/GET 接口，并由 DV-01 至 DV-05 验证完整性、时间和幂等。
2. 事件去重与目标关联器对应关联流程，维护 CatalogTarget 与 AssociationDecision，通过 DV-03、DV-04、DV-06 和 DV-07 验证稳定关联及隔离。
3. 轨道解算调度器对应解算流程，维护 SolveJob 与 OrbitSolution，实现任务创建、查询、取消及 OrbitEngine 接口，由 DV-08 至 DV-10 覆盖。
4. 残差质检状态机对应质检流程，维护 ResidualReview 并实现残差查询和复核决定接口，由 DV-11 与 DV-12 覆盖阈值及迁移。
5. 版本冻结与查询服务对应发布流程，维护 FrozenVersion 并实现冻结、历史和单版本查询接口，由 DV-13 验证互斥与不可变性。
6. 持久化及重启恢复模块贯穿全部流程，维护 EventRecordAndCheckpoint，为事务、顺序和哈希提供基础，由 DV-03 与 DV-14 验证。

## 独特性

项目不是通用任务队列或普通 CRUD 编目系统，而是将多测站异步角度弧段、确定性目标关联、可取消数值解算、三级残差质检、互斥不可变发布以及事件重放哈希校验组合成一条空间态势感知闭环。十余个独立故障场景直接围绕时间、并发、数值和恢复边界设计，使业务正确性能够被重复证明。
